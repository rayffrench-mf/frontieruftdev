// (c) Copyright 2015 - 2023 Micro Focus or one of its affiliates..
/*
 * Created on 01/02/2016.
 * The Insight factory which creates test objects by LeanFT types.
 */
var Core = require("leanft.sdk.core");
var Logger = Core.Logger;
var ObjectUtils = Core.ObjectUtils;
var ConcreteFactoryBehavior = Core.ConcreteFactoryBehavior;

var InsightTO = require("./insight.js").InsightTO;

function InsightFactory(session){
    this._logger = new Logger("Insight.TestObjectFactory");
    this._session = session;
    this._technology = "Insight";

    ObjectUtils.extend(this, ConcreteFactoryBehavior);
}

InsightFactory.prototype = {
    _logger: null,
    _session: null,
    _technology: null,

    _constructors: {
        "insight": InsightTO
    },

    _nativeClassToLeanFtType: {
        "insight": "insight"
    }
};
module.exports.InsightFactory = InsightFactory;