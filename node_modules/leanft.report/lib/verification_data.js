// (c) Copyright 2015 - 2023 Micro Focus or one of its affiliates..

/**
 * Represents the data used to report a custom verification step.
 * @function
 * @typedef {Object} VerificationData
 * @property {object} data A property bag containing possible data for the VerificationData object
 * @property {string} data.name The name of the verification presented in the report file.
 * @property {string} data.description The verification description presented in the report file.
 * @property {string} data.parameters Array of property bags with name/value pairs, representing the parameters data used in the context of the custom verification step, presented in the report file.
 * @property {string} data.image The Base64 verification image presented in the report file.
 * @returns an instance of VerificationData
 * @memberOf Reporter
 */
function VerificationData(data){
    this._name = data.name;
    this._description = data.description;
    this._args = data.parameters;
    this._image = data.image;
    this._highlightRect = data.highlightRect;
}

module.exports = VerificationData;
